function beforesubmit(){
    let outputdate = document.querySelector(".outputdate");
    let inputdate = document.querySelector(".inputdate");
    //we need to convert String -->date(en_IN) means salesforce format of current user. you can fetch Salesforce local in dev console anonomus window UserIno.getLocale() method.

}